import 'package:flutter/material.dart';

class NearestRestaurantViewMoreScreen extends StatelessWidget {
  const NearestRestaurantViewMoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Nearest Restaurants"),centerTitle: true,),

    );
  }
}
